apiVersion: v1
kind: Service
metadata:
  name: cosmos-svc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: cosmos-emulator
  spec:
  ports:
    - name: http
      port: 10251
      targetPort: 10251
    - name: https
      port: 8081
      targetPort: 8081

