apiVersion: v1
kind: Service
metadata:
  name: cosmos-svc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: cosmos-emulator
  ports:
    - port: 8081
      targetPort: 8081
