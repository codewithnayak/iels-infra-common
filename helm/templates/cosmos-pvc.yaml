apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cosmos-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storageClass }}
  resources:
    requests:
      storage: {{ .Values.cosmos.storage }}
