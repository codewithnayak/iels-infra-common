helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo update

helm upgrade --install ingress-nginx ingress-nginx/ingress-nginx \
  --namespace ingress-nginx \
  --create-namespace \
  --set controller.service.type=LoadBalancer \
  --set tcp.5432="dev-databases/postgres-svc:5432" \
  --set tcp.6379="dev-databases/redis-svc:6379" \
  --set tcp.8081="dev-databases/cosmos-svc:8081"



CRET MANAGER INSTALLATION ON GKE 
  kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.14.2/cert-manager.yaml



kubectl apply -f https://github.com/cert-manager/cert-manager/releases/latest/download/cert-manager.crds.yaml

helm repo add jetstack https://charts.jetstack.io
helm repo update

helm upgrade --install cert-manager jetstack/cert-manager \
  --namespace cert-manager \
  --create-namespace \
  --set webhook.securePort=10260 \
  --set securityContext.enabled=false \
  --set webhook.securityContext.enabled=false \
  --set cainjector.securityContext.enabled=false \
  --set startupapicheck.securityContext.enabled=false


CRETAE A CERT SECRET MANUALLY AND ATTACH IT MANUALLY 

kubectl create secret tls my-tls-secret \
  --cert=path/to/tls.crt \
  --key=path/to/tls.key \
  -n default

